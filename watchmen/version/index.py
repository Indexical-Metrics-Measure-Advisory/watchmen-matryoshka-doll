# from watchmen.common.storage.engine.storage_engine import get_client
#
# db = get_client()
#
#
# # TODO need change to replicate model
# def start_to_trace_topic_changes():
#     topic_col = db.get_collection('topic')
#     data = topic_col.watch()
#     document = next(data)
#     print(document)
